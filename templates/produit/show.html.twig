{% extends 'base.html.twig' %}

{% block title %}Détail du Produit{% endblock %}

{% block body %}
    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>📦 {{ produit.nom }}</h1>
            <div>
                <a href="{{ path('app_produit_edit', {'id': produit.id}) }}" class="btn btn-warning">
                    ✏️ Modifier
                </a>
                <a href="{{ path('app_produit_index') }}" class="btn btn-secondary">
                    ← Retour à la liste
                </a>
            </div>
        </div>

        <div class="card">
            <div class="card-body">
                <table class="table">
                    <tr>
                        <th>ID</th>
                        <td>{{ produit.id }}</td>
                    </tr>
                    <tr>
                        <th>Nom</th>
                        <td>{{ produit.nom }}</td>
                    </tr>
                    <tr>
                        <th>Description</th>
                        <td>{{ produit.description|nl2br }}</td>
                    </tr>
                    <tr>
                        <th>Prix</th>
                        <td>{{ produit.prix }} €</td>
                    </tr>
                    <tr>
                        <th>Quantité</th>
                        <td>
                            <span class="badge {% if produit.quantite < 5 %}bg-danger{% else %}bg-success{% endif %}">
                                {{ produit.quantite }}
                            </span>
                        </td>
                    </tr>
                    <tr>
                        <th>Catégorie</th>
                        <td>
                            {% if produit.categorie %}
                                <a href="{{ path('app_categorie_show', {'id': produit.categorie.id}) }}">
                                    {{ produit.categorie.nom }}
                                </a>
                            {% else %}
                                <span class="text-muted">Non catégorisé</span>
                            {% endif %}
                        </td>
                    </tr>
                    <tr>
                        <th>Créé le</th>
                        <td>{{ produit.createdAt ? produit.createdAt|date('d/m/Y H:i:s') : '' }}</td>
                    </tr>
                    <tr>
                        <th>Modifié le</th>
                        <td>{{ produit.updatedAt ? produit.updatedAt|date('d/m/Y H:i:s') : '' }}</td>
                    </tr>
                </table>
            </div>
        </div>

        <div class="mt-4">
            {{ include('produit/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}